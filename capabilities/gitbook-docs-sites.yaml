naftiko: 1.0.0-alpha2
info:
  label: GitBook API — Docs Sites
  description: 'GitBook API — Docs Sites. 5 operations. Lead operation: GitBook List docs sites. Self-contained Naftiko capability
    covering one Gitbook business surface.'
  tags:
  - Gitbook
  - Docs Sites
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITBOOK_API_KEY: GITBOOK_API_KEY
capability:
  consumes:
  - type: http
    namespace: gitbook-docs-sites
    baseUri: https://api.gitbook.com/v1
    description: GitBook API — Docs Sites business capability. Self-contained, no shared references.
    resources:
    - name: orgs-organizationId-sites
      path: /orgs/{organizationId}/sites
      operations:
      - name: listdocssites
        method: GET
        description: GitBook List docs sites
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createdocssite
        method: POST
        description: GitBook Create a docs site
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: orgs-organizationId-sites-siteId
      path: /orgs/{organizationId}/sites/{siteId}
      operations:
      - name: getdocssite
        method: GET
        description: GitBook Get a docs site
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatedocssite
        method: PATCH
        description: GitBook Update a docs site
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletedocssite
        method: DELETE
        description: GitBook Delete a docs site
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITBOOK_API_KEY}}'
  exposes:
  - type: rest
    namespace: gitbook-docs-sites-rest
    port: 8080
    description: REST adapter for GitBook API — Docs Sites. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/orgs/{organizationid}/sites
      name: orgs-organizationid-sites
      description: REST surface for orgs-organizationId-sites.
      operations:
      - method: GET
        name: listdocssites
        description: GitBook List docs sites
        call: gitbook-docs-sites.listdocssites
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createdocssite
        description: GitBook Create a docs site
        call: gitbook-docs-sites.createdocssite
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/orgs/{organizationid}/sites/{siteid}
      name: orgs-organizationid-sites-siteid
      description: REST surface for orgs-organizationId-sites-siteId.
      operations:
      - method: GET
        name: getdocssite
        description: GitBook Get a docs site
        call: gitbook-docs-sites.getdocssite
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updatedocssite
        description: GitBook Update a docs site
        call: gitbook-docs-sites.updatedocssite
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletedocssite
        description: GitBook Delete a docs site
        call: gitbook-docs-sites.deletedocssite
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gitbook-docs-sites-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitBook API — Docs Sites. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: gitbook-list-docs-sites
      description: GitBook List docs sites
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gitbook-docs-sites.listdocssites
      outputParameters:
      - type: object
        mapping: $.
    - name: gitbook-create-docs-site
      description: GitBook Create a docs site
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: gitbook-docs-sites.createdocssite
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: gitbook-get-docs-site
      description: GitBook Get a docs site
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gitbook-docs-sites.getdocssite
      outputParameters:
      - type: object
        mapping: $.
    - name: gitbook-update-docs-site
      description: GitBook Update a docs site
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: gitbook-docs-sites.updatedocssite
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: gitbook-delete-docs-site
      description: GitBook Delete a docs site
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: gitbook-docs-sites.deletedocssite
      outputParameters:
      - type: object
        mapping: $.
